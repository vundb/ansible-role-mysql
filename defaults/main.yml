#
# Root user password for mysql.
#
mysql_root_password: BRDMRJCNBYI8

#
# Custom MySQL configuration to be written to /etc/mysql/my.cnf
#
# mysql_config:
#   - { section: "mysqld",    option: "bind-address",         value: "127.0.0.1" }
#   - { section: "mysqld",    option: "innodb_log_file_size", value: "5M" }
#   - { section: "mysqldump", option: "max_allowed_packet",   value: "100M" }
#
mysql_config: []
